# Demo Flow

Phase 1 demo:

1. Start the backend.
2. Start the frontend.
3. Open the dashboard.
4. Confirm the UI loads and fetches `GET /api/health`.
5. Confirm the backend reports its environment and database status.

The wallet signature and Zero Trust authorization demo will be added in later phases.

# API

Phase 1 exposes the following routes:

- `GET /api` - basic backend readiness response
- `GET /api/health` - health snapshot used by the frontend

Future phases will add authentication, identities, devices, roles, permissions, policies, audit, and blockchain endpoints.
